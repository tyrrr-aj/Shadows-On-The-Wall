import React from 'react';
import PropTypes from 'prop-types';
import Header from "../Header/Header";

const Home = props => {
    return (
        <div>
            <Header/>
Home
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;